# Local only — copy to .env and fill values
PORT=3005
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres
# Optional: explicitly force SSL in production (Render)
PGSSLMODE=require
